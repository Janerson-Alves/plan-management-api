version: '3.8'

services:
  postgres:
    container_name: telefonia_db
    image: postgres
    ports:
      - "5434:5432"

    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=telefonia_db